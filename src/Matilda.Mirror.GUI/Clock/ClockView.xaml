<Page
    x:Class="Matilda.Mirror.GUI.Clock.ClockView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Matilda.Mirror.GUI.Clock"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{StaticResource clockViewModel}">

    <Grid Background="Black">
        <StackPanel VerticalAlignment="Top">
            <TextBlock
                HorizontalAlignment="Center"
                FontSize="30"
                Text="{Binding Path=CurrentTime, FallbackValue=Time will be shown here}" Foreground="White" 
                />
            <TextBlock
                HorizontalAlignment="Center"
                FontSize="30"
                Text="{Binding Path=CurrentDate, FallbackValue=Date will be shown here}" Foreground="White" 
                />
        </StackPanel>

        <StackPanel HorizontalAlignment="Left" Margin="30,253,0,760" Width="397">
            <TextBlock x:Name="Notification"  TextWrapping="Wrap" Text="Notification" TextAlignment="Center" Foreground="#FFE6E4E4"/>
        </StackPanel>

        <StackPanel HorizontalAlignment="Right" Margin="0,253,32,760" Width="406">
            <TextBlock x:Name="Music" TextWrapping="Wrap" Text="Music" TextAlignment="Center" Foreground="White"/>
        </StackPanel>

        <StackPanel HorizontalAlignment="Left" Margin="37,856,0,173" Width="390">
            <TextBlock x:Name="Amentities" TextWrapping="Wrap" Text="Amentities" TextAlignment="Center" Foreground="#FFFFFBFB"/>
        </StackPanel>

        <StackPanel HorizontalAlignment="Right" Margin="0,856,32,173" Width="406">
            <TextBlock x:Name="Closet" TextWrapping="Wrap" Text="Closet" TextAlignment="Center" Foreground="White"/>
        </StackPanel>

    </Grid>
</Page>